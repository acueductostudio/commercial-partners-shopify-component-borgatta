{
  "buildCommand": "parcel build src/index.js --dist-dir dist --public-url ./ && find dist -name '*.js' -type f ! -name '*.map' | head -1 | xargs -I {} mv {} dist/cotizacion-unificado.js && find dist -name '*.css' -type f ! -name '*.map' | head -1 | xargs -I {} mv {} dist/cotizacion-unificado.css",
  "outputDirectory": "dist",
  "framework": null,
  "installCommand": "npm install",
  "devCommand": "npm run dev",
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "Access-Control-Allow-Origin",
          "value": "*"
        }
      ]
    }
  ]
}
